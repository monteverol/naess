/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        'darkorchid-turtle-430545.hostingersite.com',
        // Add any other domains you might load images from
      ],
    },
    // Your other configurations...
  };
  

export default nextConfig;